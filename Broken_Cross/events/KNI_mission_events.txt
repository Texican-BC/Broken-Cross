# Triggered by missions in the KNI/LAT tree

namespace = KNI_LAT_events

# Sardinia mission reward
country_event = {
	id = KNI_LAT_events.1
	title = KNI_LAT_events.1.t
	desc = KNI_LAT_events.1.d
	picture = CONQUEST_eventPicture
	
	is_triggered_only = yes
	
	option = { # Prepare to Conquer Majorca
		name = KNI_LAT_events.1.a
		baleares_area = {
			limit = {
				NOT = { is_core = ROOT }
				NOT = { is_permanent_claim = ROOT }
			}
			add_permanent_claim = ROOT
		}
		if = {
			limit = { NOT = { exists = MJO } }
			add_country_flag = KNI_option_MJO
		}
	}
	option = { # Prepare to Free Majorca (Majorca still exists)
		name = KNI_LAT_events.1.b
		trigger = { exists = MJO religion = chalcedonism }
		add_historical_friend = MJO
		MJO = { add_liberty_desire = 50 add_historical_friend = KNI }
	}
}